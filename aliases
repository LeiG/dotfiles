# attach to an existing session or create a new one if none is found
moshdev () {
  SERVER=$1
  COLO=$2
  /Users/$USER/bin/mosh -6 \
  --client=/Users/$USER/bin/mosh-client \
  --server=/home/$USER/bin/mosh-server \
  $SERVER.$COLO.facebook.com -- tmux -2CC new-session -A -s $SERVER$COLO
}
